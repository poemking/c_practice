#include<stdio.h>
#include<string.h>
int main(int argc, char* argv[]){
	char input[80];
	size_t length;

	puts("請輸入一個字串...");
	gets(input);

	length=strlen(intput);
	printf("字串長度:%u\n",length);
	return 0;
}
